// Normally I hate modifying prettier defaults as they're usually nitpicky, but Solana Kit tends to use longer names, 
// so we need to increase the print width to avoid single function calls spilling over into multiple lines

export default {
  printWidth: 120,
};
